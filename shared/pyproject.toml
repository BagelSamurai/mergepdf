[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mergepdf-shared"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pypdf>=4.0.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["mergepdf_shared*"]